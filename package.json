{
  "name": "tenequm-plugins",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-marketplace": "bash scripts/sync-marketplace.sh",
    "validate": "pnpm -r validate && pnpm claude plugin validate .",
    "changeset:version": "changeset version && pnpm sync-marketplace",
    "release": "pnpm sync-marketplace && pnpm validate && changeset tag"
  },
  "devDependencies": {
    "@anthropic-ai/claude-code": "^2.0.34",
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.7"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@10.20.0"
}
